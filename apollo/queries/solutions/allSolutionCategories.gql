query SolutionCategories($id: ID!) {
    solutionCategories(where: { slug: $id }) {
        id
        slug
        name
        solutions {
            id
            title
            slug
            image {
                url
            }
        }
    }
}
