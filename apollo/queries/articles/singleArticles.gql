query Articles($id: ID!) {
    articles(where: { slug: $id }) {
        id
        title
        slug
        excerpt
        body
        created_at
        Author
        image {
            url
            name
            width
            height
        }
        categories {
            name
            id
        }
    }
}
